export enum PaymentMethod {
  PAGAR_ME = 'pagar_me',
  STRIPE = 'stripe',
  PAYPAL = 'paypal',
  ASAAS = 'asaas',
}

export enum OrderStatus {
  PENDING = 'pending',
  FAILED = 'failed',
  CANCELLED = 'cancelled',
}
